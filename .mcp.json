{
  "mcpServers": {
    "toulmin": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/start.sh",
      "args": ["--review-config", "${CLAUDE_PLUGIN_ROOT}/review.json", "--no-persist"]
    },
    "browser": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/start-browser.sh"
    }
  }
}
